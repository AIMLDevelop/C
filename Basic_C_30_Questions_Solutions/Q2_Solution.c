// Write a program to accept a number as input from the user and print it.
#include <stdio.h>

int main(){
    int a;
    printf("Enter a integer to print : ");
    scanf("%d",&a);
    printf("The number is %d",a);
    return 0;
}